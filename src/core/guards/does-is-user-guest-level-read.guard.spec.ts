import { DoesIsUserGuestLevelReadGuard } from './does-is-user-guest-level-read.guard';

describe('DoesIsUserGuestLevelReadGuard', () => {
  it('should be defined', () => {
    expect(new DoesIsUserGuestLevelReadGuard()).toBeDefined();
  });
});
